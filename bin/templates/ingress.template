apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: echomap
spec:
  rules:
  - host: {{ HOST }}
    https:
      paths:
      - path: /
        backend:
          serviceName: {{ SERVICE_NAME }}
          servicePort: {{ SERVICE_PORT }}
