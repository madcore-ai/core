apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ HOST }}
spec:
  rules:
  - host: {{ HOST }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ SERVICE_NAME }}
          servicePort: {{ SERVICE_PORT }}
